Array|Array is a collection of elements stored in contiguous memory locations.|An array is one of the most basic data structures used in computer science. It stores elements of the same data type in contiguous memory locations. Each element in an array can be accessed using an index value. Arrays allow constant time access to elements because their memory location can be calculated directly. They are commonly used for storing lists of data and performing operations such as searching and sorting. However, arrays have a fixed size in many programming languages, which means their capacity must be defined in advance. Arrays are widely used as the underlying structure for many other data structures.|images/array.png|array data structure contiguous memory indexed access

Linked List|Linked list is a linear data structure where elements are connected using pointers.|A linked list is a linear data structure where elements are stored in nodes. Each node contains two parts: the data and a reference to the next node. Unlike arrays, linked lists do not store elements in contiguous memory locations. This allows dynamic memory allocation and flexible size changes. Linked lists support efficient insertion and deletion operations compared to arrays. However, accessing elements requires traversal from the head node. Variants of linked lists include singly linked lists, doubly linked lists, and circular linked lists. Linked lists are widely used in memory management and implementation of other data structures.|images/linked_list.png|linked list nodes pointers dynamic memory linear structure

Stack|Stack is a linear data structure that follows the Last In First Out principle.|A stack is a linear data structure that follows the Last In First Out (LIFO) principle. This means the element inserted last will be removed first. The main operations performed on a stack are push, pop, and peek. Push adds an element to the top of the stack, while pop removes the top element. Peek returns the top element without removing it. Stacks are widely used in expression evaluation, syntax parsing, and function call management. They can be implemented using arrays or linked lists. Stack operations are efficient and usually run in constant time.|images/stack.png|stack lifo push pop data structure

Queue|Queue is a linear data structure that follows the First In First Out principle.|A queue is a linear data structure that follows the First In First Out (FIFO) principle. This means the first element inserted into the queue will be the first one removed. The primary operations of a queue are enqueue and dequeue. Enqueue inserts an element at the rear of the queue, while dequeue removes an element from the front. Queues are widely used in scheduling systems and buffering applications. Examples include printer queues and CPU process scheduling. Queues can be implemented using arrays, linked lists, or circular arrays. The operations of a queue are efficient and usually take constant time.|images/queue.png|queue fifo enqueue dequeue scheduling

Tree|Tree is a hierarchical data structure consisting of nodes connected by edges.|A tree is a hierarchical data structure composed of nodes connected by edges. The topmost node is called the root, and nodes connected below it are called children. Each node may have zero or more child nodes. Trees are widely used to represent hierarchical data such as file systems and organizational structures. Common types of trees include binary trees, binary search trees, and AVL trees. Tree traversal methods such as preorder, inorder, and postorder are used to visit nodes in different orders. Trees allow efficient searching, insertion, and deletion operations in many applications.|images/tree.png|tree hierarchical data structure nodes root children

Graph|Graph is a data structure consisting of vertices connected by edges.|A graph is a non-linear data structure used to represent relationships between objects. It consists of vertices, also called nodes, and edges that connect pairs of vertices. Graphs can be directed or undirected depending on whether edges have direction. They can also be weighted when edges carry numerical values. Graphs are widely used in applications such as social networks, transportation networks, and communication systems. Common graph operations include traversal, searching, and path finding. Graphs can be represented using adjacency matrices or adjacency lists.|images/graph.png|graph vertices edges network structure

Bubble Sort|Bubble sort is a simple comparison-based sorting algorithm.|Bubble sort repeatedly compares adjacent elements in an array and swaps them if they are in the wrong order. The algorithm continues this process until no more swaps are required. During each pass, the largest unsorted element moves toward the end of the array. Because of this behavior, the algorithm is called bubble sort. Although it is easy to understand and implement, bubble sort is inefficient for large datasets. The algorithm performs many unnecessary comparisons and swaps. Its average and worst-case time complexity is O(n²). Bubble sort is mainly used for educational purposes rather than real-world applications.|images/bubble_sort.png|bubble sort sorting algorithm simple sort comparison swap

Selection Sort|Selection sort repeatedly selects the smallest element and places it at the correct position.|Selection sort works by repeatedly finding the minimum element from the unsorted portion of the array. Once the smallest element is found, it is swapped with the element at the beginning of the unsorted section. This process continues until the entire array becomes sorted. Selection sort divides the array into sorted and unsorted regions. During each iteration, the smallest element is selected from the unsorted region. Although the algorithm is simple, it is not efficient for large datasets. The time complexity of selection sort is O(n²). However, it performs fewer swaps compared to bubble sort.|images/selection_sort.png|selection sort sorting algorithm minimum element comparison sorting

Insertion Sort|Insertion sort builds the sorted array one element at a time.|Insertion sort works similarly to how cards are arranged in a hand during a card game. The algorithm takes one element from the unsorted portion and inserts it into its correct position in the sorted portion. Initially, the first element is considered sorted. Each new element is compared with the previous elements and inserted into its proper position. This process continues until all elements are sorted. Insertion sort performs well for small datasets or nearly sorted arrays. It is stable and easy to implement. The average and worst-case time complexity of insertion sort is O(n²).|images/insertion_sort.png|insertion sort sorting algorithm card sorting incremental sorting

Dijkstra Algorithm|Dijkstra algorithm finds the shortest path from a source node to all other nodes in a graph.|Dijkstra's algorithm is used to compute the shortest path between nodes in a weighted graph. It starts from a source node and gradually explores neighboring nodes. At each step, the algorithm selects the node with the smallest known distance. The distances to adjacent nodes are updated if a shorter path is found. This process continues until the shortest path to all nodes is determined. Dijkstra's algorithm works only for graphs with non-negative edge weights. It is widely used in navigation systems and network routing. The time complexity depends on the data structure used for implementation.|images/dijkstra.png|dijkstra shortest path graph algorithm weighted graph routing

Minimum Spanning Tree|A minimum spanning tree connects all vertices of a graph with minimum total edge weight.|A minimum spanning tree (MST) is a subset of edges in a connected weighted graph that connects all vertices without cycles. The total weight of all edges in the tree is minimized. MST algorithms are used in network design and optimization problems. Two popular algorithms used to find MST are Kruskal's algorithm and Prim's algorithm. These algorithms select edges in a way that avoids cycles while connecting all vertices. The result is a tree structure that spans all nodes with minimum cost. MST ensures efficient network connections with minimal resource usage.|images/mst.png|minimum spanning tree mst graph algorithm kruskal prim

Topological Sort|Topological sorting orders vertices of a directed acyclic graph.|Topological sorting is used to arrange the vertices of a directed acyclic graph in a linear order. In this order, every directed edge from vertex A to vertex B ensures that A appears before B. Topological sorting is useful in scheduling tasks where certain tasks must be completed before others. The algorithm can be implemented using depth-first search or using a queue-based method. It works only on directed graphs without cycles. If a cycle exists, topological sorting is not possible. Applications include course scheduling and dependency resolution.|images/topological_sort.png|topological sort dag graph ordering dependency scheduling


Segment Tree|Segment tree is a data structure used for range queries.|A segment tree is a binary tree used for storing intervals or segments. It is commonly used to answer range queries efficiently. Examples of such queries include finding the sum, minimum, or maximum of elements within a specific range. The segment tree divides the array into segments and stores information about each segment in nodes. Each internal node represents a combination of values from its child nodes. Segment trees allow fast updates and queries. Both operations typically run in O(log n) time. Segment trees are widely used in competitive programming and real-time data processing.|images/segment_tree.png|segment tree range query data structure interval tree

Fenwick Tree|Fenwick tree is a data structure used to efficiently compute prefix sums.|Fenwick tree, also known as a Binary Indexed Tree, is used for efficiently calculating prefix sums in an array. It allows both updates and queries to be performed quickly. The structure stores cumulative frequency information in a compact way. Each node represents a range of elements in the array. By using bit manipulation techniques, Fenwick trees can update values and compute prefix sums efficiently. The time complexity for both update and query operations is O(log n). Fenwick trees are widely used in competitive programming for handling cumulative frequency tables.|images/fenwick_tree.png|fenwick tree binary indexed tree prefix sum range query

Dynamic Programming|Dynamic programming solves complex problems by breaking them into overlapping subproblems.|Dynamic programming is an algorithmic technique used to solve optimization problems efficiently. It works by breaking a problem into smaller overlapping subproblems. The results of these subproblems are stored so that they can be reused later. This avoids repeated computations and significantly improves efficiency. Dynamic programming can be implemented using memoization or tabulation. Many classic problems such as the Fibonacci sequence, knapsack problem, and longest common subsequence use dynamic programming. The technique is widely used in optimization and decision-making problems.|images/dynamic_programming.png|dynamic programming dp optimization subproblems memoization

Greedy Algorithm|Greedy algorithms make locally optimal choices to find a global optimum.|Greedy algorithms solve problems by making the best possible choice at each step. The idea is to select the option that appears most beneficial at the moment. These choices are made without reconsidering previous decisions. Greedy algorithms work well for problems that exhibit the greedy-choice property. Examples include activity selection, Huffman coding, and minimum spanning tree algorithms. Although greedy methods are efficient, they do not always guarantee optimal solutions for every problem. However, when applicable, they provide simple and fast solutions.|images/greedy_algorithm.png|greedy algorithm optimization local choice scheduling

Union Find|Union Find is a data structure used to keep track of elements divided into disjoint sets.|Union Find, also known as the Disjoint Set Union data structure, is used to manage a collection of disjoint sets. It supports two main operations called union and find. The union operation merges two sets together, while the find operation determines which set a particular element belongs to. This structure is very useful in problems involving connectivity and grouping. Union Find is widely used in graph algorithms such as Kruskal's algorithm for finding minimum spanning trees. Path compression and union by rank are common optimizations used to improve efficiency. These techniques significantly reduce the time required for operations. With these optimizations, operations run in nearly constant time.|images/union_find.png|union find disjoint set dsu connectivity graph algorithm

Kruskal Algorithm|Kruskal algorithm finds a minimum spanning tree of a weighted graph.|Kruskal's algorithm is a greedy algorithm used to find the minimum spanning tree of a connected weighted graph. The algorithm works by sorting all edges of the graph by weight. It then repeatedly adds the smallest edge that does not create a cycle in the growing tree. Union Find is commonly used to detect cycles efficiently during the process. The algorithm continues until all vertices are connected with the minimum total edge weight. Kruskal's algorithm is widely used in network design and clustering problems. It is efficient and simple to implement. The time complexity mainly depends on sorting the edges.|images/kruskal.png|kruskal minimum spanning tree mst graph greedy algorithm

Prim Algorithm|Prim algorithm constructs a minimum spanning tree by expanding from a starting node.|Prim's algorithm is another method used to find the minimum spanning tree of a weighted graph. It starts from an arbitrary vertex and grows the spanning tree step by step. At each step, the algorithm adds the smallest edge that connects a vertex inside the tree to a vertex outside the tree. This process continues until all vertices are included in the spanning tree. Prim's algorithm typically uses a priority queue to efficiently select the next minimum edge. It is widely used in network design and optimization problems. The algorithm guarantees that the resulting tree has the minimum possible weight. Its time complexity depends on the data structures used.|images/prim.png|prim algorithm mst graph greedy spanning tree

Knuth Morris Pratt Algorithm|KMP algorithm efficiently searches for a pattern inside a text.|The Knuth Morris Pratt algorithm is a string searching algorithm used to find occurrences of a pattern in a text. It improves the naive string matching method by avoiding unnecessary comparisons. The algorithm preprocesses the pattern to build a longest prefix suffix array. This array helps determine how much the pattern can shift when a mismatch occurs. Instead of restarting the comparison from the beginning, KMP uses previously computed information. This significantly reduces the number of comparisons needed. The algorithm works efficiently even for very long texts. The overall time complexity of KMP is O(n + m), where n is the text length and m is the pattern length.|images/kmp.png|kmp string matching pattern searching algorithm

Rabin Karp Algorithm|Rabin Karp uses hashing to search for patterns in text.|Rabin Karp is a string searching algorithm that uses hashing to find patterns in text efficiently. The algorithm computes a hash value for the pattern and compares it with hash values of substrings in the text. If the hash values match, a direct character comparison is performed to verify the match. This approach reduces the number of unnecessary comparisons. Rabin Karp is particularly useful when searching for multiple patterns in a large text. The algorithm uses a rolling hash technique to update hash values efficiently. Although collisions may occur, proper hash functions reduce their probability. The average time complexity of Rabin Karp is O(n + m).|images/rabin_karp.png|rabin karp string hashing pattern search rolling hash

Backtracking|Backtracking is an algorithmic technique used to solve problems by exploring all possibilities.|Backtracking is a problem-solving technique used to explore all possible solutions for a problem. The algorithm builds solutions incrementally and abandons a path as soon as it determines that the path cannot lead to a valid solution. This pruning of the search space helps reduce unnecessary computations. Backtracking is widely used in problems such as the N-Queens problem, Sudoku solving, and permutation generation. The technique systematically explores candidate solutions using recursion. Although backtracking may have high computational complexity, pruning strategies help improve efficiency. The algorithm ensures that all possible valid solutions are explored. Backtracking is an important concept in combinatorial optimization.|images/backtracking.png|backtracking recursion search n queens sudoku algorithm

Branch and Bound|Branch and Bound is an optimization technique used for solving combinatorial problems.|Branch and Bound is an algorithm design technique used to solve optimization problems efficiently. It works by dividing the problem into smaller subproblems called branches. Each branch represents a possible solution space. The algorithm calculates bounds for each branch and eliminates branches that cannot produce better solutions. This helps reduce the number of possibilities that need to be explored. Branch and Bound is commonly used for problems such as the traveling salesman problem and job scheduling. The method ensures that the optimal solution is eventually found. Compared to brute force methods, it significantly reduces the search space.|images/branch_bound.png|branch and bound optimization combinatorial search algorithm

Suffix Array|Suffix array is a data structure used for efficient string searching.|A suffix array is a data structure that stores the starting positions of all suffixes of a string in sorted order. It allows efficient searching of patterns within a text. By using binary search on the suffix array, pattern matching can be performed quickly. Suffix arrays are widely used in text processing, bioinformatics, and data compression. They provide a memory-efficient alternative to suffix trees. Once constructed, they allow fast substring queries. Building a suffix array requires sorting suffixes of a string. The structure helps solve many advanced string matching problems.|images/suffix_array.png|suffix array string processing substring search

Suffix Tree|Suffix tree is a compressed trie containing all suffixes of a string.|A suffix tree is a tree-based data structure that represents all suffixes of a given string. It is constructed in a compressed form to reduce memory usage. Each path from the root to a leaf represents a suffix of the string. Suffix trees allow very fast pattern searching and substring queries. They are widely used in bioinformatics, data compression, and text analysis. The structure can solve many string problems efficiently. Although suffix trees are powerful, they require complex construction algorithms. Once built, pattern searches can be performed in linear time.|images/suffix_tree.png|suffix tree compressed trie string indexing substring search

AVL Tree|AVL Tree is a self-balancing binary search tree.|AVL Tree is a type of self-balancing binary search tree. It ensures that the height difference between the left and right subtrees of any node is at most one. This difference is called the balance factor. Whenever the tree becomes unbalanced after insertion or deletion, rotations are performed to restore balance. These rotations include left rotation, right rotation, left-right rotation, and right-left rotation. Because the tree remains balanced, operations such as search, insertion, and deletion remain efficient. AVL Trees guarantee logarithmic height. The time complexity for search, insertion, and deletion operations is O(log n).|images/avl_tree.png|avl tree balanced tree self balancing bst rotations

Red Black Tree|Red Black Tree is a self-balancing binary search tree using node colors.|Red Black Tree is a type of self-balancing binary search tree where each node has a color attribute, either red or black. The tree follows specific rules to maintain balance. For example, the root must always be black and red nodes cannot have red children. These rules ensure that the longest path in the tree is not more than twice the length of the shortest path. Rotations and recoloring are used during insertion and deletion to maintain these properties. Because the tree remains balanced, operations remain efficient. Red Black Trees are widely used in operating systems and database indexing. The time complexity for search, insertion, and deletion is O(log n).|images/red_black_tree.png|red black tree balanced bst rotations tree

Heap Tree|Heap Tree is a specialized tree-based data structure that satisfies the heap property.|A heap tree is a complete binary tree that follows a special ordering property called the heap property. In a max heap, the value of each parent node is greater than or equal to its children. In a min heap, the value of each parent node is smaller than its children. Heap trees are commonly implemented using arrays instead of linked nodes. They are widely used in priority queues and heap sort algorithms. Insertion and deletion operations maintain the heap structure using heapify operations. Heap trees allow efficient retrieval of the maximum or minimum element. The time complexity of insertion and deletion operations is O(log n).|images/heap_tree.png|heap tree max heap min heap priority queue

Huffman Tree|Huffman Tree is used for efficient data compression.|Huffman Tree is a binary tree used in Huffman coding for data compression. It is constructed based on the frequency of characters in the input data. Characters with lower frequency are placed deeper in the tree, while frequent characters appear closer to the root. The tree is built using a greedy algorithm that repeatedly merges the two least frequent nodes. Once the tree is constructed, binary codes are assigned to each character. These codes are used to compress data efficiently. Huffman coding reduces the total number of bits required to represent data. It is widely used in file compression formats such as ZIP and multimedia encoding.|images/huffman_tree.png|huffman tree data compression encoding greedy algorithm

B Tree|B Tree is a self-balancing tree used in databases and file systems.|A B Tree is a balanced search tree designed for systems that read and write large blocks of data. Unlike binary trees, a B Tree node can have multiple children. This reduces the height of the tree and improves efficiency for disk-based storage. B Trees maintain sorted data and allow searches, insertions, and deletions in logarithmic time. They are widely used in databases and file systems for indexing data. Each node contains multiple keys and pointers to child nodes. This structure minimizes disk accesses and improves performance. The time complexity of search, insertion, and deletion operations is O(log n).|images/b_tree.png|b tree database indexing balanced tree

Splay Tree|Splay Tree is a self-adjusting binary search tree.|A splay tree is a self-adjusting binary search tree that moves frequently accessed elements closer to the root. Whenever a node is accessed, a series of rotations called splaying is performed. This operation moves the accessed node to the root of the tree. As a result, recently accessed elements become quicker to access again. Splay trees do not maintain strict balance like AVL trees or Red Black Trees. Instead, they rely on access patterns to improve performance. Over time, frequently accessed nodes remain near the top of the tree. The amortized time complexity for operations is O(log n).|images/splay_tree.png|splay tree self adjusting tree rotations access optimization

Priority Queue|Priority Queue is a data structure where elements are processed according to their priority.|A priority queue is an abstract data structure similar to a regular queue but with an additional priority assigned to each element. Elements with higher priority are removed before elements with lower priority. Unlike a normal queue that follows FIFO order, priority queues process elements based on priority values. They are widely used in scheduling algorithms and network routing. Many implementations of priority queues use heap structures for efficiency. Insertion and deletion operations maintain the priority ordering. Priority queues are used in algorithms such as Dijkstra’s shortest path algorithm. The time complexity for insertion and deletion operations is typically O(log n).|images/priority_queue.png|priority queue heap scheduling data structure

Binomial Heap|Binomial Heap is a collection of binomial trees used to implement priority queues.|A binomial heap is a heap structure made up of a collection of binomial trees. Each binomial tree follows the heap property where the parent node is smaller or larger than its children depending on the heap type. The trees in the heap follow a specific order based on their degree. Binomial heaps allow efficient merging of two heaps, which makes them useful in applications requiring frequent heap union operations. Operations such as insertion, deletion, and extract-min are supported efficiently. The structure is commonly used in advanced priority queue implementations. The time complexity of most operations is logarithmic.|images/binomial_heap.png|binomial heap priority queue heap trees merging heaps

Fibonacci Heap|Fibonacci Heap is an advanced heap structure used for efficient priority queue operations.|A Fibonacci heap is a data structure consisting of a collection of heap ordered trees. It is mainly used to implement priority queues efficiently. The structure allows very fast insertion and decrease-key operations. Trees are merged lazily, which helps achieve better amortized time complexity. Fibonacci heaps are particularly useful in graph algorithms such as Dijkstra's algorithm and Prim's algorithm. Although the structure is complex, it provides excellent theoretical performance. The decrease-key operation is extremely efficient compared to other heap structures. Fibonacci heaps achieve amortized O(1) insertion and decrease-key operations.|images/fibonacci_heap.png|fibonacci heap priority queue graph algorithms

Trie|Trie is a tree-based data structure used for storing and searching strings efficiently.|A trie, also known as a prefix tree, is a data structure used to store a dynamic set of strings. Each node in the trie represents a character of a word. Words are stored by linking characters from the root to leaf nodes. This structure allows efficient searching of words and prefixes. Tries are commonly used in autocomplete systems and spell checking applications. They support fast insertion and search operations based on string length. Many dictionary and search engine systems use tries for indexing words. The time complexity for insertion and search operations is proportional to the length of the word.|images/trie.png|trie prefix tree string search dictionary

Compressed Trie|Compressed Trie is an optimized trie that reduces the number of nodes.|A compressed trie is a variation of the standard trie data structure that reduces unnecessary nodes. In a normal trie, each character occupies a separate node, which can increase memory usage. Compressed tries combine chains of single-child nodes into one node. Instead of storing individual characters, nodes store substrings. This significantly reduces the height and size of the tree. Compressed tries are useful in applications such as dictionaries and search engines. They allow efficient prefix searching while using less memory. The search complexity remains proportional to the length of the query.|images/compressed_trie.png|compressed trie prefix tree optimized trie string indexing

Suffix Tree|Suffix Tree is a compressed tree structure that stores all suffixes of a string.|A suffix tree is a specialized tree structure that contains all suffixes of a given string. It is a compressed version of a trie that stores suffixes instead of full words. Each path from the root to a leaf represents a suffix of the string. Suffix trees allow very fast pattern searching and substring queries. They are widely used in text processing, bioinformatics, and pattern matching problems. Once constructed, many string operations can be performed efficiently. Although construction can be complex, searching becomes extremely fast. The time complexity for substring search using suffix trees is linear in the length of the pattern.|images/suffix_tree.png|suffix tree string processing substring search indexing